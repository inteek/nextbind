if (window['jQuery'] != undefined) {

    jQuery(function () {
        "use strict";

        _init();

        function initSignIn() {

        }
        function _init() {
            initSignIn();
        }

        //$(document).ready(ActivarCuenta("Hola"));

        //function ActivarCuenta(mensaje)
        //{
        //    debugger
        //    console.log(mensaje);
        //    conectarAsy("CambiarEstatus", null, function (result) {
        //        debugger
        //        if (result.error == true) {
        //            alert(result.msg);
        //        }
        //    });
        //}
    });
}